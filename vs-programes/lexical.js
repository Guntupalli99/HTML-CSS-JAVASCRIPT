function names() {
    const heros = ['naveen', 'babu', 'balu', 'maharshi', 'athreya']

    function actor() {
        for (let actor of heros) {
            console.log(heros);
        }
    }
    actor();
}
